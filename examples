{
  "protocol_version": "1.3.0",
  "mode": "adaptive",
  "settings": {
    "autonomic_level": "high",
    "agility_factor": 0.95
  },
  "description": "Basic AAAP configuration template."
}
# AAAP Protocol Quick Usage Example

import json
# Assuming validator.py is in the main directory
# from validator import validate_config 

def run_aaap_process():
    with open('examples/protocol_config.json', 'r') as f:
        config = json.load(f)
    
    print(f"Starting AAAP in {config['mode']} mode...")
    # result = validate_config(config)
    # print(f"Validation Result: {result}")

if __name__ == "__main__":
    run_aaap_process()
